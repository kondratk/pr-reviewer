# pr_reviews

A macOS menu bar app for reviewing pull requests.

## Install & Update

Run the install script from the project root:

```bash
./install.sh
```

This will:

1. Close the app if it's currently running
2. Build a Release build via `xcodebuild`
3. Copy the app to `/Applications`
4. Launch the app
